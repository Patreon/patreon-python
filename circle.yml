machine:
  timezone: America/Los_Angeles


dependencies:
  override:
    - pip install coveralls
    - pip install -e .


test:
  override:
    - py.test --cov=patreon -rssX -q patreon
    - coveralls

